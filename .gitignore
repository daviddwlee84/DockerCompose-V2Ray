# secrets
.env
.vault-pass
*.vault-pass

# runtime state (generated by deploy)
server/runtime/
logs/

# legacy / transitional
data/

# ansible
*.retry
ansible/inventory/prod.ini
ansible/inventory/dev.ini
ansible/inventory/*.local.*
# Real vault file (lives beside vault.yml.example). Should be ansible-vault
# encrypted, but gitignored as a defense-in-depth in case encryption is
# skipped.
ansible/group_vars/*/vault.yml

# azure-validation helpers: throwaway secrets + generated client configs
.secrets/
out/

# test fixtures (local-only)
test/ssh/id_ed25519
test/ssh/id_ed25519.pub
test/ssh/known_hosts

# editor / OS
.DS_Store
.idea/
.vscode/
