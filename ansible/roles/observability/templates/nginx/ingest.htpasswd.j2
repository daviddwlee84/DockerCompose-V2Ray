{# Basic-auth file for the /prom and /loki ingest endpoints. nginx supports the
   {PLAIN} scheme (RFC 2307) in auth_basic_user_file. The same password already
   lives in plaintext in each agent's config.alloy, so {PLAIN} here is consistent;
   swap for a `openssl passwd -apr1` hash if you want it hashed at rest. #}
{{ obs_ingest_user }}:{PLAIN}{{ obs_ingest_password }}
